---
description: Do not start dev servers/watchers (user runs them).
alwaysApply: true
---

## Dev servers / watchers

- Don’t start long-running dev servers/watchers (e.g. `npm run dev`).
- Assume the user has them running with auto-reload.
- If a restart is needed, tell the user the command—don’t run it.

