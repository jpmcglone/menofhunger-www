---
description: Keep feed concepts consistent across API, web, and iOS
globs: "composables/use*Feed*.ts,composables/useFeed*.ts,composables/useUrlFeedFilters.ts,composables/composer/**/*.ts,composables/useComposerMedia.ts,pages/index.vue,pages/home.vue,pages/feeds.vue,pages/new-posts.vue,pages/only-me.vue,pages/g/**,pages/u/**,pages/admin/media-review/**,components/app/feed/**,components/app/Feed*.vue"
alwaysApply: false
---

# Feed Surface

Feeds should converge on one shared mental model:

- Content tabs: `posts`, `replies`, `media`.
- Ordering: `new` / `trending`.
- Audience/filtering: `visibility` where the surface supports it.
- Pagination: cursor-based `{ data, pagination.nextCursor }`.

Before adding a feed mode, query param, collapse behavior, media endpoint, or realtime feed event, ask whether it can be expressed with these concepts. If not, state why the new concept is needed and which clients will consume it.

Use canonical entity fields across clients; snapshots and named partial patches follow the [realtime policy](../../docs/engineering-policy.md#realtime-contracts-and-ownership).
