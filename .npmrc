# Nuxt typecheck spawns vue-tsc. Pass the build heap budget through npm/npx
# to that child too; an option on `nuxt build` alone does not cover typecheck.
# Render starts Nitro directly with node, outside this npm configuration.
node-options=--max-old-space-size=6144
