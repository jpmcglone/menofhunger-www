<template>
  <!-- Tab bar content wrapper: owns its own padding + max-width. -->
  <div class="mx-auto w-full max-w-6xl px-2 pt-2 pb-1 h-[var(--moh-tabbar-height,4.5rem)] flex items-end">
    <slot />
  </div>
</template>

